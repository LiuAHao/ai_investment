# 2026-08-04

## 修复虚拟环境与前端启动问题

- 虚拟环境 `.venv` 损坏：此前用 PATH 中 CodeBuddy 内置 python3（可重定位构建，`sys.base_prefix=/install`）创建，导致 `ModuleNotFoundError: No module named 'encodings'`。
- 修复：删除 `.venv`，改用绝对路径 `/Users/liu/.workbuddy/binaries/python/versions/3.11.9/bin/python3 -m venv .venv` 重建，依赖安装成功。
- 关键经验：本机创建 venv 必须用 workbuddy Python 的绝对路径，不能用 PATH 里的 `python3`。

## Node 版本

- 前端 Vite 7 要求 Node `^20.19.0 || >=22.12.0`，系统 PATH 默认是 node@18 (v18.20.8)。
- nvm 已安装 v20.20.2、v22.23.1，需 `nvm use 22` 后重新 `npm install`。

## V2 Agent 开关

- `.env` 中 `AGENT_V2_ENABLED=true` 已开启，后端 V2 路由（`/api/agent/v2/query` 等）才注册，否则前端调用返回 404。

## 前端去 V2 化（发行版本统一）

用户要求产品不区分 V1/V2，对外不暴露 V2 概念。已完成：
- `ResearchChatView.jsx`：删除「投资周期」「调试模式」UI/state/提交参数（`debug_mode`、`investment_horizon`），注释去掉 V2
- `SettingsView.jsx`：删除「默认投资周期」「默认进入 V2 分析」「调试模式」
- `apiV2Service.js`：404 错误文案不再含 V2/AGENT_V2_ENABLED 字样
- `HistoryView.jsx`：删除 `session.period` 标签
- `LandingView.jsx`：删除 "V2 · 过程可视化" 徽标
- 后端 `/api/agent/v2/*` 路由与 apiV2Service 内部实现保留，仅前端 UI 隐藏 V2 概念
- 根目录 `index.html` 是独立原型文件（非 Vite 入口，入口为 `src/web/index.html`），未改动

## 研究工作台展示重构（ResearchChatView.jsx）

用户评估了前后端对应关系后要求精简展示：
- **真实可展示**：总体结论 summary、关键判断 key_points、证据池（真实检索）、工具执行、Critic/合规校验
- **模板化/形式展示（已删除）**：情景分析 scenarios（固定30/40/30模板）、可选操作 action_options（完全固定模板）、主要风险固定文案
- **已完成**：
  - 完成后收起 6 阶段流程画布，仅分析中（analyzing）显示
  - 完成后的 turn 渲染为可折叠卡片，展开显示「分析结论 + 证据」（不再渲染流程画布）
  - 证据面板去掉置信度百分比（规则打分易误导）
  - 「主要风险 + 数据限制 + 合规声明」合并为「风险与限制」区域
  - 删除校验记录面板（Critic/合规）、scenario-card CSS
- 前端 `npm run build` 验证通过

## 多 Agent 重构需求（进行中，2026-08-04）

用户决定将项目从"静态工作流编排"重构为"真正多 Agent 系统"，已输出《docs/多Agent重构设计方案.md》待评审。核心方向：
- 保留 OrchestratorAgent（总编排），改为 LLM 驱动调度（非写死节点）
- 三个调研 Agent（Market/News/Knowledge）为真正 ReAct Agent：LLM 自主思考→选工具→观察→循环，不复用静态 planner
- SummaryAgent 总结分析 Agent：综合调研结论 + 可再调工具核实
- 复用资产：ToolRegistry（11 个工具）、统一资产模型 state.py、build_client()、SSE 事件总线、前端会话骨架
- 前端从"6阶段流程画布"改为"Agent 卡片"（agent_thinking / tool_started / tool_completed 事件驱动）
- 待确认决策：并行 vs 串行、SummaryAgent 工具权限、旧 V2 图回退切换、DeepSeek function calling 兼容性、旧历史会话兼容

## 重构决策已确认（2026-08-04 下午）

方案《docs/多Agent重构设计方案.md》v2.0，5 个决策全部确认：
1. 三个调研 Agent **并行**（asyncio.gather）
2. SummaryAgent **给工具权限**（可再调工具核实）
3. **不保留旧代码**，demo 形态直接删（V1/V2 图、AGENT_V2_ENABLED 开关、旧前端组件）
4. DeepSeek function calling 需 JSON 解析兜底
5. **旧对话不保留**，前端完全重构

补充设计（用户要求）：
- **编排 Agent 也展示思考过程**（`orchestrator_thinking` 事件 + OrchestratorCard 组件）
- **按需调度**：前端只渲染实际启动的调研 Agent 卡片（如"市场环境"只启动 Market+News，不渲染 Knowledge），无"等待调度"状态
- 前端原型：`docs/前端展示原型.html`（可用 python3 -m http.server 8899 预览）

目标架构：OrchestratorAgent(LLM调度) → 并行 MarketAgent/NewsAgent/KnowledgeAgent(均 BaseReActAgent ReAct循环) → SummaryAgent(综合+可调工具) → 最终结论
事件协议：orchestrator_thinking / orchestrator_decided / agent_started / agent_thinking / tool_started / tool_completed / agent_completed / final_answer

## 设计方案 v3.1 评审修正（2026-08-04）

对方案做了交叉审查（对照现有代码），发现并修正 8 个问题：
- **P1 执行模型**：build_client() 是同步 OpenAI 客户端，asyncio 并行会退化为串行 → 改用 ThreadPoolExecutor(max_workers=3) 线程池并行，Agent.run() 为同步方法（决策#9）
- **P2 state 迁移顺序**：tools/base.py、tools/registry.py 依赖旧 agent/v2/state.py → 阶段1必须先迁 agents/state.py + 改 tools import + 验证，再删旧 agent/ 目录
- **P3 编排Agent工具权限**：Orchestrator 需少量资产解析/搜索工具（"茅台"→600519）
- **P4 渲染时序**：前端只以 orchestrator_decided.plan 为准创建卡片，后续事件仅更新状态
- **P5 会话存储**：内存会话不落库（决策#12），历史记录页显示进程内会话
- **P6 事件协议**：补充 tool_failed / agent_failed 事件 + 任务级总超时
- **P7 工具命名**：保留现有工具名（StockDataTool 等），不做无收益改名（决策#11）
- **P8 事件归集**：所有事件带 agent 字段，前端按 agent 分组归集

工具层现有文件：stock_tools/news_tools/knowledge_tools/fund_tools/etf_tools/us_stock_tools/analysis_tools，11 个工具已注册（StockDataTool/StockSpotTool/NewsSearchTool/KnowledgeQueryTool/AnalysisTool/FundNavTool/FundProfileTool/EtfProfileTool/EtfTrackingTool/UsStockQuoteTool/UsStockHistoryTool）

## RAG 内容策略（决策#13，已写入方案 6.3）

用户确认 RAG 内容策略写入方案。核心结论：
- **现状问题**：RAG 只有 6 个静态教材文档（财务指标/估值框架/技术分析/行业模板/A股规则/合规话术），网络可免费获取且更好，增量≈0，所以 KnowledgeAgent 没用
- **差异化定位**：RAG 只装"网络给不了的 4 类增量"——私有/可信内容、确定性事实（指标口径/监管原文可溯源）、结构化深度知识（研报精华）、Agent 工具书
- **内容挑选标准**：网络能搜到同款→淘汰；时效性高→淘汰；需精确引用→进 RAG；私有/沉淀→进 RAG
- **分层更新**：L1 静态知识（数月，手动）/ L2 定期知识（周月，定时任务批量摄入）/ L3 沉淀知识（实时，研究完成自动写回 RAG，Agent 自学习）
- **落地**：删 6 个教材文档 → 导入合规规则库+个股/行业档案+分析框架；KnowledgeAgent 从"查教材"改为"查档案+沉淀经验"；保留现有 Chroma+embedding+rerank 管线（只换内容）；新增 rag/indexer.py add_knowledge() 写入接口
- 现有 RAG 原始目录：src/rag/data/raw/（6个md），入口 src/rag/knowledge_tool.py（查工具名 KnowledgeQueryTool）

## 方案 v3.1 第二轮评审修正（2026-08-04）

新增决策 #14 #15 + 修正 P9 P10：
- **决策#14 删除登录/用户系统**：demo 无登录，删除 auth/JWT/models/user.py/AuthView/登录路由/quota.py
- **决策#15 数据源目录**：src/data/ 合并旧 src/stock/+src/news/；src/asset/（AssetMasterData 资产主数据）保留；旧目录删除
- **P9 目录遗漏 src/asset/**：asset_resolver 依赖 AssetMasterData（名称→代码搜索），必须保留
- **P10 登录系统删除**（与 demo 定位不符）
- **R1/R2 资产识别设计**：不重复造轮子——封装现有 asset_resolver 规则逻辑为 AssetResolveTool（tools/asset_tools.py），编排 Agent 通过工具调用完成资产识别，复用 asset/resolver.py + AssetMasterData
- 目录新增：src/asset/（AssetMasterData/resolver.py/data）、tools/asset_tools.py
- 前端无 AuthView，App.jsx 直接进入研究页
- 实施阶段5 补充删除：登录系统、src/stock/、src/news/

## ★ 多 Agent 重构实施完成（2026-08-04 深夜）

**已交付完整可运行产物**，验证通过：
- 后端：`PYTHONPATH=src python src/api/main.py` 可启动，路由 `/api/agent/query`、`/api/agent/events/<task_id>`、`/api/history` 全部可用
- 全链路实测：提交"市场环境怎么样"→ 编排 Agent 正确按需调度 Market+News → 线程池并行 → SSE 事件流正常 → 内存会话保存正常
- 前端：`npm run build` 通过（37 modules，CSS 5.54kB）
- 唯一待用户操作：`.env` 中 DEEPSEEK_API_KEY 是占位符（your-deepseek-api-key），需填真实 Key；建议加 `AGENT_LLM_MODEL=deepseek-chat`

## run.py 智能 Node 版本切换（2026-08-04 深夜修复）

用户 `python run.py --frontend` 又报 Node 18 不兼容（Vite 7 需要 20.19+/22.12+），原因是 `shutil.which("npm")` 找到 homebrew 的 node@18。
**已永久修复**：重写 run.py，内置 Node 版本检测：
- `parse_node_version()` / `node_version_ok()` 判断 Vite 7 版本要求
- `node_bin_dirs()` 收集候选：PATH + nvm（`~/.nvm/versions/node/v*`）+ homebrew（`node@*`），**按版本号降序优先新版**
- `find_compatible_node()` 返回第一个满足要求的 bin 目录
- `start_frontend()`：当前 node 满足→直接用；不满足→自动切换 nvm v22 并提示
- 实测：v18.20.8 → 自动切 v22.23.1 → Vite 7.3.5 ready in 103ms ✅

## 多 Agent 运行问题修复（2026-08-04 深夜，重要）

首轮端到端测试发现问题并全部修复，系统已可完整运行（市场环境问题 → 编排调度 → Market+News 收敛 → Summary 高质量结论）：

1. **`response.content` 错误**（orchestrator.py + loop.py）：OpenAI SDK 响应应为 `response.choices[0].message.content`，否则编排走规则兜底（plan 为空）
2. **function calling 空 tool_calls 死循环**（loop.py）：模型直接给文本回答时 `[] is not None` 为 True 进入空转 → 重写 `_try_function_calling()` 返回文本/空串/None 三态，`run()` 区分处理
3. **工具无 input_schema**（tools/base.py）：所有工具 schema 为空，模型不知如何传参 → 新增 `_infer_input_schema()` 从 execute 签名自动生成 + `coerce_params()` 字符串数字转 int（**关键坑**：`from __future__ import annotations` 使注解变字符串，必须用 `typing.get_type_hints` 解析）
4. **工具名→代码解析缺失**（tools/stock_tools.py）：`cn_stock_history("上证指数")` 失败 → 新增 `_resolve_symbol()` 用 asset master 名称→代码
5. **收敛机制**（loop.py + prompts）：新增 `_check_progress()` 单调性检测（成功工具数不增→提前收敛）+ `_force_converge()`；各 Agent prompt 加"最多调 2-3 个工具、拿数据立即 FINAL"收敛指令
6. **web_search 类型 bug**（utils/web_search.py + news_tools.py + registry.execute）：`web_limit="5"` 字符串 → ddgs 内部 `"5"/10` 报错 → search_web 内 int 转换 + registry 统一 coerce_params
7. **RAG 降级**（rag/knowledge_tool.py）：embedding 模型无法下载（huggingface 连不上）→ `_load()` 失败不致命，`query()` 降级纯关键词检索（`mode: keyword_only`），新增 `_build_result()` 统一返回

## tushare 数据源接入（2026-08-04）

- 新增 `src/data/tushare_client.py`：`get_daily()`（日线K线）、`get_spot_summary()`（实时行情），token 认证更稳定
- `.env` 新增 `TUSHARE_TOKEN` 配置；`requirements.txt` 加 tushare
- `stock_client.py` 回退链：**tushare → 东方财富 → 腾讯 → 新浪**；`fetch_spot_em` 也 tushare 优先
- 未配置 token 时优雅降级（`_INIT_FAILED` 缓存），不影响 akshare
- **待用户操作**：填入真实 TUSHARE_TOKEN 才能用 tushare 源（当前是占位符）

## SSE 事件格式修复（2026-08-04 深夜）

用户反馈前端 OrchestratorAgent 显示"已完成"但思考内容空、plan 空。根因是**前端 SSE 解包层级错误**：
- 后端事件结构：`{type, task_id, timestamp, data:{业务数据}}`，SSE 帧的 data = 完整事件对象
- 前端 `apiClient.js` 的 handler 之前 `data: e`（把整个事件对象当业务数据）→ 改为 `data: e.data`
- 连带修复：`api/events.py` 的 `_sse()`（connected/heartbeat/快照补发）原本直接序列化裸数据，格式与实时事件不一致 → 统一为 `{type, data}` 结构
- 事件总线 `event_bus.py` 加**每任务历史缓冲**（MAX_BUFFER=200），SSE 连接建立晚于事件发射时也能补发（订阅时回放缓冲）

验证：`orchestrator_thinking`（data.thought 正确）、`orchestrator_decided`（data.plan 数组正确）、task_completed 快照补发正常。"分析贵州茅台" 完整链路：三路并行调研 → SummaryAgent 高质量结论（技术面 MA20/MA60 + 提价事件 + 行业周期）✅

## 历史会话完整流程保存与展示（2026-08-04 深夜）

用户要求历史会话展示完整 Agent 流程（过程调用 + 子 Agent 分析），不只最终结论：
- **后端**（api/routes/agent.py `_save_session_turn`）：`agent_results` 原来只存 agent_name/conclusion/thinking_log/evidence_refs，**丢失 tool_calls** → 现在完整保存 tool_calls（tool_name/status/latency_ms/error/summary 摘要截断），新增 `_tool_data_summary()` 辅助函数；**踩坑**：函数定义插到 try/except 中间导致 SyntaxError，修复后语法验证通过
- **前端**（新增 components/agents/TurnDetail.jsx + 重构 views/HistoryView.jsx）：历史详情从"只显示 answer"改为渲染 `TurnDetail` —— 问题头 + 调研过程（每个 Agent 卡片：💭 思考流 + ✅/❌ 工具调用 + 📝 Agent 结论）+ 最终报告（SummaryAgent + 总体判断/关键判断/风险/数据限制/合规）
- 验证：market 环境问题保存了 MarketAgent（1思考+7工具调用）+ NewsAgent（1思考+1工具调用）+ 完整结论 ✅

## 提示词框架重写（研究深度升级，2026-08-04 深夜）

用户反馈两轮后研究深度不深，诊断 4 个缺陷：缺研究框架/收敛过激进/输出无模板/无示例。全部重写 4 个 Agent 的 system prompt：
- **统一模式**：角色 → 【研究框架】分析维度清单 → 【工具使用指南】何时用哪个 → 【工作方式】 → 【输出格式】FINAL 模板 → 【收敛原则】覆盖维度即收 → 【合规】
- MarketAgent：6 个维度（量价/指数强弱/技术形态/估值/历史对比/财务），收敛从"2-3工具就收"改为"覆盖核心维度（3-5次）"
- NewsAgent：5 个维度（关键事件/时间线/多方观点/倾向/情绪映射），2-3 轮不同关键词
- KnowledgeAgent：按知识类型分维度（框架/合规/口径/方法），"知识库未覆盖就如实标注"
- SummaryAgent：分析框架（交叉验证/逻辑链/多空权衡/风险/时间框架），JSON 输出新增 reasoning/information_gaps/time_frame 字段
- 配套：state.py InvestmentAnswer 新增 reasoning/information_gaps/time_frame；summary_agent 解析新字段 + `_extract_time_frame` 兜底；前端 SummaryCard.jsx + TurnDetail.jsx 展示新字段
- 实测：茅台分析深度明显提升（营收1720亿/净利823亿/30亿回购/两次提价/行业调整期β拖累，逻辑链完整）✅
- **遗留**：数据源仍不稳定（东方财富断连/腾讯空/新浪慢），MarketAgent 8 次迭代被消耗在重试上；KnowledgeAgent 因 embedding 模型缺失走关键词降级，6 次迭代耗尽——建议用户填 TUSHARE_TOKEN 缓解行情问题，RAG 需网络或预下载 bge 模型

## RAG 向量模型安装完成（2026-08-04 深夜）

用户已填 TUSHARE_TOKEN，huggingface 网络已通。选择策略（尽量小）：
- **embedding**：`BAAI/bge-small-zh-v1.5`（92MB，512维，中文效果好的最小模型之一）→ 已下载到 `~/.cache/huggingface/hub/models--BAAI--bge-small-zh-v1.5`
- **rerank**：`bge-reranker-base` 1.1GB 太大 → 配置 `rag_config.json` 中 `rerank.enabled=false` 关闭
- 重新构建索引：prepare_chunks（31块）→ build_chroma_index（31条）→ build_index
- 验证：检索"市盈率的计算方法和口径差异" → 模式 `hybrid`（非降级 keyword_only），首个结果精确命中"指标口径 A股/美股" ✅
- 端到端：茅台分析结论深度提升（"业绩筑底、估值重构"、二次提价催化剂、消费税变量、时间框架短期）✅
- **观察**：SummaryAgent 输出的 JSON 字段不完整（reasoning/risks/information_gaps 常为空，time_frame 有值）——模型行为，非代码 bug，可通过 prompt 强化字段必填（可选优化）

## 前端展示优化：结论位置 + Markdown 渲染（2026-08-04 深夜）

用户反馈：①结论产出应显示在卡片底部而非顶部；②MD 格式内容需渲染，不能整段纯文本。
- **新增 `components/common/Markdown.jsx`**：零依赖轻量 Markdown 渲染器（支持 #标题/**粗体*/*斜体*/-列表/1.列表/`代码`/>引用/---分隔线/[链接]），`dangerouslySetInnerHTML` 转义防 XSS；样式加在 global.css（.md-render）
- **AgentCard.jsx**：布局改为"思考流💭 → 工具调用🔧 → **结论📝固定在底部**（分隔线+浅绿背景）"，思考/结论用 Markdown 渲染
- **SummaryCard.jsx**：总结/推理/时间框架用 Markdown 渲染；关键判断/风险/信息缺口逐条渲染
- **TurnDetail.jsx**（历史详情）：同步 Markdown 渲染
- 构建验证通过（CSS 6.42kB + JS 216kB）
- **经验**：write_to_file 对 Markdown.jsx 又出现 0 字节，重写后正常（137行）——凡是新增文件写入后必须先 wc -l 验证

**实施要点（重要经验）**：
- 子代理用 code-explorer（只读）无法写文件，改为主代理亲自实现 + 子代理做探索验证
- **write_to_file 偶发写入 0 字节**！遇到 ImportError/文件空时先 `wc -l` 检查再重写（state.py、summary_agent.py、indexer.py、global.css 都踩过坑）
- 工具真实注册名是小写 snake_case（cn_stock_history/asset_news_search 等），不是类名（StockDataTool）——白名单配置必须用注册名
- 新增 `src/tools/asset_tools.py` 的 `AssetResolveTool`（注册名 asset_resolve），封装 asset/resolver.py 规则
- 新增 `src/agents/events.py` 事件协议（orchestrator_thinking/orchestrator_decided/agent_*/tool_*/final_answer）
- RAG 内容已替换：删除财务指标/技术分析/估值框架/行业模板 4 个教材，新增指标口径说明+分析框架与工具手册；保留 A股规则+合规话术；新增 rag/indexer.py add_knowledge() L3 沉淀
- 旧代码已全删：src/agent/（V1+V2）、api/auth.py、api/agent_v2.py、api/stock/news/chat/feedback/eval_api、models/、eval/、news/、services 旧文件
- 迁移：llm_common→utils/llm_common.py、data_agent→data/stock_client.py、news_agent→data/news_client.py
- 前端完全重构：components/agents/（OrchestratorCard/AgentCard/SummaryCard/AgentWorkspace）、hooks/useAgentStream.js、services/apiClient.js、styles/global.css；删旧组件
- 会话为内存态（agents/memory.py），历史页显示进程内会话

**前端展示结构**：输入区 → OrchestratorCard（思考流+派发计划）→ AgentWorkspace（动态列，只渲染 plan 中 Agent）→ SummaryCard（总体判断/关键判断/主要风险/证据引用/合规）

## tushare 覆盖度补全（2026-08-04 深夜，重要）

用户反馈"配了 tushare 但日志仍走其他源"。诊断出 3 层根因并已全部修复：

**根因**：
1. 若用系统 python3（非 .venv）启动，`import tushare` 失败 → `_INIT_FAILED=True` 进程内永禁 → 全量走 akshare
2. tushare 覆盖度极低：只有 A股日线+实时行情 2 个入口，指数/ETF/基金/周月线/复权全部缺失
3. 降级是静默的，日志无数据源标注

**tushare token 权限实测**（可用）：daily / pro_bar(复权·周月线) / index_daily / fund_daily / fund_basic / fund_nav / daily_basic / index_dailybasic / trade_cal / stock_basic / index_global / hk_daily(限频1次/小时) / index_basic
**无权限**（保留 akshare）：us_daily / news / major_news / stk_factor

**修复内容**：
- `src/data/tushare_client.py` 整体重写（600行）：统一 `get_daily(symbol, adjust, freq, kind)` 自动分流股票/指数/ETF/基金/港股；新增 get_daily_basic/get_index_daily_basic(估值)/get_fund_profile/get_fund_nav/get_index_global(恒生·纳指·标普)/get_hk_daily/get_trade_calendar；`_INIT_FAILED` 改为 300s 冷却重试；`_ts_code` 场内(.SH/.SZ)·场外(.OF) 判断修正
- `src/data/stock_client.py`：`_fetch_hist_with_fallback` 支持周/月线+复权走 tushare、kind 透传、每级降级打 warning 标注数据源；新增 fetch_valuation()
- `src/tools/stock_tools.py`：StockDataTool 支持 INDEX/ETF，`_infer_kind()` 用 asset master 区分指数/ETF/股票（解决 000001 平安银行 vs 上证指数歧义），返回含 valuation
- `src/tools/fund_tools.py` / `etf_tools.py`：tushare 优先，akshare 兜底
- `us_stock_tools.py`：us_daily 无权限，保留 akshare 并注释说明
- `market_agent.py` prompt 更新：cn_stock_history 支持指数/ETF

**实测全链路通过**：股票/指数/ETF 日周月线+复权、场内外基金信息与净值、恒生/纳指、估值 PE/PB 全部 tushare 取数成功；后端 create_app 正常。**遗留**：hk_daily 接口 1次/小时 限频（需更高积分）；指数 000xxx 自动推断时先试股票再试指数会有一条 warning（正常兜底流程）。
