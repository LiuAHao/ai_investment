# AI投资分析系统 - 生产环境问题与解决方案

## 项目架构

- **后端**：Flask + SQLAlchemy + SQLite/MySQL
- **前端**：React + Vite
- **Agent系统**：多Agent协作（MasterAgent、StockAgent、NewsAgent、AnalysisAgent）
- **RAG系统**：ChromaDB + sentence-transformers
- **并发模型**：Python threading（非异步）

---

## 一、高并发访问

### 问题
- `threading.Thread` 无限制创建线程，100个用户同时分析 = 100个线程 + 100个数据库连接
- SQLite 并发写入会锁库
- Agent 执行器全局字典 `_executors` 只增不减，内存持续增长

### 已实施方案
1. **线程池限制** — `src/api/agent.py`
   - 使用 `ThreadPoolExecutor(max_workers=10)` 替代 `threading.Thread`
   - 新增容量检查，满载时返回 503（系统繁忙）

2. **数据库连接池优化** — `src/models/__init__.py`
   - SQLite：`StaticPool` + `check_same_thread=False`，解决多线程连接安全
   - MySQL/PG：`QueuePool(pool_size=10, max_overflow=20, pool_pre_ping=True)`，自动管理连接池和断线重连
   - Session 改为 `scoped_session`，线程安全

3. **执行器自动清理** — `src/api/agent_executor.py`
   - 后台守护线程每 5 分钟扫描，已完成/失败超过 1 小时的执行器自动从内存移除

---

## 二、用户频繁操作

### 问题
- 快速连续点击"分析"会创建多个重复任务
- 无输入长度限制，可能遭受长文本攻击

### 已实施方案
1. **重复任务检测** — `src/api/agent.py`
   - 基于 MD5(user_id + symbol + query) 生成任务指纹
   - 30 秒内相同请求直接拒绝（429）

2. **输入验证与长度限制** — `src/api/agent.py`
   - `MAX_QUERY_LENGTH = 2000`，`MAX_SYMBOL_LENGTH = 20`
   - 超长输入返回 400

---

## 三、资源滥用防护

### 问题
- 无用户级使用限制，可能耗尽 LLM API 配额
- 无法区分不同级别用户

### 已实施方案
1. **用户等级模型** — `src/models/database.py`
   - User 表新增 `user_tier` 字段：`free` / `pro` / `premium`

2. **配额管理系统** — `src/utils/quota_manager.py`
   - 内存计数 + `threading.Lock` 线程安全，按自然日自动重置
   - 配额设定：

   | 等级 | 深度分析/天 | 智能问答/天 |
   |------|------------|------------|
   | 免费版 | 5 | 20 |
   | 专业版 | 30 | 100 |
   | 旗舰版 | 100 | 500 |

3. **配额拦截点**
   - `src/api/agent.py` — `/analyze` 和 `/query` 路由检查 `analysis_per_day`
   - `src/api/chat.py` — `/ask` 路由检查 `chat_per_day`
   - 超限统一返回 429

4. **后端 API 支撑** — `src/api/main.py`、`src/api/auth.py`
   - `GET /api/user/quota` — 查询当前配额使用情况
   - `GET /api/user/tiers` — 获取所有等级信息
   - `POST /api/user/upgrade` — 升级用户等级
   - 登录/注册响应携带 `user_tier` 字段

5. **前端会员入口** — `ProfileView.jsx`、`apiClient.js`、`App.jsx`
   - 个人资料页：等级徽章 + 今日配额进度条 + 三档会员对比卡片 + 升级按钮
   - 顶部菜单：显示当前等级，非旗舰版用户显示"升级会员"快捷入口

---

## 四、待优化项（未实施）

| 方向 | 说明 | 优先级 |
|------|------|--------|
| API 熔断器 | LLM/AKShare 调用失败时自动熔断，防止级联故障 | 中 |
| 分布式锁 | 多实例部署时用 Redis 实现任务互斥 | 低（单实例暂不需要） |
| 优雅关闭 | SIGTERM 信号处理，等待进行中任务完成后退出 | 中 |
| 监控指标 | 请求量/错误率/响应时间等指标收集与告警 | 中 |
| 日志轮转 | 配置日志文件大小和数量限制 | 低 |
| 异步框架迁移 | 考虑 FastAPI + asyncio 提升并发能力 | 长期 |

---

## 五、部署检查清单

- [x] 配置线程池大小（`AGENT_MAX_WORKERS` 环境变量）
- [x] 配置数据库连接池（SQLite/MySQL 自动适配）
- [x] 设置输入长度限制
- [x] 启用重复任务检测
- [x] 配置用户配额限制
- [x] 执行器自动清理
- [ ] 配置熔断器参数
- [ ] 启用健康检查监控
- [ ] 配置日志轮转
- [ ] 设置监控告警
- [ ] 测试优雅关闭
